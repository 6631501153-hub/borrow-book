// lib/staff_history.dart
import 'package:flutter/material.dart';

class StaffHistoryPage extends StatelessWidget {
  const StaffHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      // Staff History will show the history of ALL assets (per the table of functions)
      child: Text('Staff History (All Assets) Page (Coming Soon)'),
    );
  }
}